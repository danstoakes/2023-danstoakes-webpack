import React from "react";

import * as styles from "./index.module.scss";

const Footer = () => {
    return (
        <footer>
            <h1>Footer</h1>
        </footer>
    );
};

export default Footer;